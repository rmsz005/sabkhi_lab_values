---
apiVersion: v1
kind: Service
metadata:
  name: navidrome-tailscale
  namespace: media
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "navidrome"
    tailscale.com/tags: "tag:k8s"
    tailscale.com/proxy-to: "http://navidrome.media.svc.cluster.local:4533"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/name: navidrome
  ports:
  - port: 80
    targetPort: 4533
    protocol: TCP
    name: http
