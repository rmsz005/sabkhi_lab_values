---
# ServiceMonitor for Immich Server metrics
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: immich-server
  namespace: monitoring
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: server
spec:
  jobLabel: app.kubernetes.io/name
  selector:
    matchLabels:
      app.kubernetes.io/instance: immich
      app.kubernetes.io/name: server
  namespaceSelector:
    matchNames:
      - immich
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      scheme: http

---
# ServiceMonitor for Immich Machine Learning metrics
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: immich-machine-learning
  namespace: monitoring
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: machine-learning
spec:
  jobLabel: app.kubernetes.io/name
  selector:
    matchLabels:
      app.kubernetes.io/instance: immich
      app.kubernetes.io/name: machine-learning
  namespaceSelector:
    matchNames:
      - immich
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      scheme: http

